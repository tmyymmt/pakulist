{
  "name": "pakulist-local-web-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "正規取得済みの投稿データをブラウザ内で検出・証拠出力するローカルWebアプリ",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/detection.js && node --check public/app.js",
    "bench": "node benchmarks/detection-performance.mjs",
    "bench:check": "node benchmarks/detection-performance.mjs --assert",
    "test:e2e": "playwright test",
    "test:all": "npm run check && npm test && npm run bench:check && npm run test:e2e"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
